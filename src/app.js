const express = require('express');
const app = express();
const morgan = require('morgan');

app.use(morgan('dev'))


// Middleware 
    
// Database

//Routes

// Handle Error

module.exports = app;